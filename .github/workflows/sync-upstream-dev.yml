name: Sync upstream dev into fork dev (merge)

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    env:
      NEXT_PUBLIC_KAKAO_REDIRECT_URI: ${{ secrets.NEXT_PUBLIC_KAKAO_REDIRECT_URI }}
      NEXT_PUBLIC_API_BASE_URL: ${{ secrets.NEXT_PUBLIC_API_BASE_URL }}

    steps:
      - name: Checkout fork dev
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.SYNC_TOKEN }}
          ref: dev
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Add upstream
        run: |
          if git remote | grep -q upstream; then
            git remote set-url upstream https://github.com/StudiGo-community/Frontend.git
          else
            git remote add upstream https://github.com/StudiGo-community/Frontend.git
          fi

      - name: Fetch upstream
        run: git fetch upstream

      - name: Merge upstream/dev into fork/dev
        run: git merge upstream/dev --no-edit

      - name: Push fork dev
        run: git push origin dev
